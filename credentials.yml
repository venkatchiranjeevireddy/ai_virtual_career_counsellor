# Credentials for connected services
# This file is for connecting to external messaging channels.
# We are using the REST channel for the Streamlit app, which requires no credentials.

rest:
#  # This endpoint is used for hosting the Rasa server as a REST channel.
#  # It is the default endpoint for the Rasa API.
